{    
    "less.compile": {
        "main": "build.less",
        "compress":  true,  // true => remove surplus whitespace
        "sourceMap": false,  // true => generate source maps (.css.map files)
        "out":       "../css/theme.css" // false => DON'T output .css files (overridable per-file, see below)
    }
}